# Database Configuration
DB_HOST=your-database-host
DB_USER=your-database-user
DB_PASS=your-database-password
DB_NAME=nightscape

# Server Configuration
PORT=8080
DOMAIN=nightscapes.io

# SSL Certificate Paths (optional, uses default Let's Encrypt paths if not set)
# NIGHTSCAPE_PRIVATE_KEY_FILE=/path/to/privkey.pem
# NIGHTSCAPE_PUBLIC_KEY_FILE=/path/to/fullchain.pem
